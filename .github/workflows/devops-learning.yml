name: Day 3 Jobs Demo

on:
  push:

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1
        run: echo "This is the first job"

  second-job:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1
        run: echo "This is the second job"

