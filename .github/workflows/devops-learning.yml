name: Variables Demo

on:
  push:

env:
  APP_NAME: BillingAPI

jobs:
  show-variable:
    runs-on: ubuntu-latest

    steps:
      - name: Display Variable
        run: echo "Application Name is $APP_NAME"

